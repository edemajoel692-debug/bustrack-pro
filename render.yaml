services:
  - type: web
    name: bustrack-pro
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: PORT
        value: 10000
    disk:
      name: bustrack-db
      mountPath: /data
      sizeGB: 1
